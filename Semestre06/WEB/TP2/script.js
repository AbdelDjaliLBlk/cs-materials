//Exo4:

// let table = Création d’un tableau contenant différents types : number, string, boolean.
// alert() = Affiche le contenu du tableau (converti en chaîne).
// tab.push(6.7) = Ajoute un élément à la fin du tableau.
// tab.pop() = Supprime le dernier élément du tableau.
// tab.unshift(false) = Ajoute un élément au debut du tbleau.
// tab.shift() = Supprime le premier élément du tableau.
// let index = tab.indexOf("Bonjour") = Recherche la position de "Bonjour" dans le tableau.
// tab.splice(index, 1) = Supprime 1 élément à partir de la position "index".
// let sousTableau = tab.slice(1, 3) = Crée un nouveau tableau contenant les éléments de index 1 --> 2
// tab.reverse() = Inverse l’ordre des éléments du tableau.
// tab.lenght =  Donne le nombre total d’éléments du tableau.
// tab.map(function(element){...}) = map parcourt chaque élément et retourne un NOUVEAU tableau.
// tab.filter(function(element){}) = filter retourne tableau contenant les éléments qui respectent la condition.


//Exo5

//Types de fonctions:
// Fonctions Déclarées (Classiques).
// Fonctions Anonymes.
// Foins Fléchées.
// Fonctions Immédiatement Exécutées (IIFE).
// Fonctions Expressions.

// Différence entre f3 et f4 : 
// f3 est le resulat  d'une exécution mais f4 est une fonction ,
// Donc f3 contient un nombre (900) ,pour f4 elle doit etre appeler, ex:f4(100), 
// console.log(f3(30)) provoque une erreur car f3 n’est pas une fonction.

// Fonctions immédiatement exécutées (IIFE) :
// f1,f2,f3,f6 sont des fonctions immédiatement exécutées (IIFE).
// On les reconnaît:
//  * La fonction est entourée de parenthèses.
//  * Elle est suivie immédiatement de () pour l’exécuter.